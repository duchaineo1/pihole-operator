{{- if .Values.rbac.enable }}
# This rule is not used by the project pihole-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the pihole-operator.org.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: piholednsrecord-editor-role
rules:
- apiGroups:
  - pihole-operator.org
  resources:
  - piholednsrecords
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - pihole-operator.org
  resources:
  - piholednsrecords/status
  verbs:
  - get
{{- end -}}
