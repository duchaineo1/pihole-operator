---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: piholednsrecords.pihole-operator.org
spec:
  group: pihole-operator.org
  names:
    kind: PiholeDNSRecord
    listKind: PiholeDNSRecordList
    plural: piholednsrecords
    shortNames:
    - dnsrec
    singular: piholednsrecord
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: DNS hostname
      jsonPath: .spec.hostname
      name: Hostname
      type: string
    - description: Record type
      jsonPath: .spec.recordType
      name: Type
      type: string
    - description: IP address
      jsonPath: .spec.ipAddress
      name: IP
      priority: 1
      type: string
    - description: CNAME target
      jsonPath: .spec.cnameTarget
      name: CNAME
      priority: 1
      type: string
    - description: Last sync time
      jsonPath: .status.lastSyncTime
      name: Last Sync
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PiholeDNSRecord is the Schema for the piholednsrecords API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PiholeDNSRecordSpec defines the desired state of PiholeDNSRecord
            properties:
              cnameTarget:
                description: CNAMETarget is the target hostname for CNAME records
                type: string
              description:
                description: Description is a human-readable description of this DNS
                  record
                maxLength: 200
                type: string
              hostname:
                description: Hostname is the DNS name to create a record for
                maxLength: 253
                minLength: 1
                type: string
              ipAddress:
                description: IPAddress is the IP address for A or AAAA records
                type: string
              recordType:
                description: RecordType is the type of DNS record (A, AAAA, or CNAME)
                enum:
                - A
                - AAAA
                - CNAME
                type: string
            required:
            - hostname
            - recordType
            type: object
          status:
            description: PiholeDNSRecordStatus defines the observed state of PiholeDNSRecord
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the PiholeDNSRecord's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the last time the DNS record was successfully
                  synced
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
