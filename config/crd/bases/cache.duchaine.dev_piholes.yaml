---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: piholes.cache.duchaine.dev
spec:
  group: cache.duchaine.dev
  names:
    kind: Pihole
    listKind: PiholeList
    plural: piholes
    singular: pihole
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Pihole is the Schema for the piholes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Pihole
            properties:
              adminPassword:
                description: |-
                  AdminPassword is the password for the Pi-hole web interface
                  If not provided, a random password will be generated
                type: string
              adminPasswordSecretRef:
                description: |-
                  AdminPasswordSecretRef references an existing Secret containing the admin password.
                  When set, the operator will not create a secret and will use this reference instead.
                properties:
                  key:
                    default: password
                    description: Key is the key within the secret to use
                    type: string
                  name:
                    description: Name is the name of the secret
                    type: string
                required:
                - name
                type: object
              dnsLoadBalancerIP:
                description: DnsLoadBalancerIP is the IP to use for DNS LoadBalancer
                  service type
                type: string
              dnsServiceType:
                default: NodePort
                description: DnsServiceType is the Kubernetes service type for DNS
                  (ClusterIP, NodePort, LoadBalancer)
                enum:
                - ClusterIP
                - NodePort
                - LoadBalancer
                type: string
              image:
                default: docker.io/pihole/pihole:2025.11.0
                description: Image is the Pi-hole container image to use
                type: string
              size:
                default: 1
                description: Size is the number of pihole instances (replicas)
                format: int32
                type: integer
              storageClass:
                description: StorageClass is the storage class to use for the PVC
                type: string
              storageSize:
                default: 1Gi
                description: StorageSize is the size of the persistent volume for
                  Pi-hole data
                type: string
              timezone:
                default: UTC
                description: Timezone for Pi-hole (e.g., "America/New_York", "Europe/London")
                type: string
              webLoadBalancerIP:
                description: WebLoadBalancerIP is the IP to use for Web LoadBalancer
                  service type
                type: string
              webServiceType:
                default: ClusterIP
                description: WebServiceType is the Kubernetes service type for Web
                  UI (ClusterIP, NodePort, LoadBalancer)
                enum:
                - ClusterIP
                - NodePort
                - LoadBalancer
                type: string
            type: object
          status:
            description: status defines the observed state of Pihole
            properties:
              adminPasswordSecret:
                description: AdminPasswordSecret is the name of the secret containing
                  the admin password
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              readyReplicas:
                description: ReadyReplicas is the number of ready replicas
                format: int32
                type: integer
              serviceName:
                description: ServiceName is the name of the service created for this
                  Pi-hole instance
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
